object Test
  abstract class Triple[A, B, C]
  case class MkTriple(x: A, y: B, z: C) extends Triple

  // def f[A,B] (x: Triple[A, A, A]): Int(32) := 5 end f

  def f[A] (x: A): A := x end f
  // f(5);
  // f(false)
  
  def g[A,B] (x: B): B := f(x) end g

  // f(MkTriple((), (), ())) match {
  //   case _ => 4
  // };
  // f(MkTriple(false, false, false)) match {
  //   case _ => 4
  // }

  // def three[X] (x: X): Triple[X,X,X] :=
  //   MkTriple(x,x,x)
  // end three
  //
  // def getFirst[X, Y, Z] (t: Triple[X, Y, Z]): X :=
  //   t match {
  //     case MkTriple(x, y, z) => z
  //   }
  // end getFirst
  //
  // three(4) == MkTriple(4, (), 6);
  // getFirst(three(false))

  val y: Int(32) = 5; y match {
    case x => x
  }
end Test
